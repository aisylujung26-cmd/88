pip install pytest
pip install --upgrade pip
pip install -r requirements.txt

pytest
pytest -v
pytest -s
pytest -s -v
pytest test_pytest.py
pytest --version

pytest # запуск тестов
pytest -v # подр. описание про тест
pytest -s # выведет принт с тестом
pytest --version # показ версию